---
variant: fcos
version: 1.5.0

ignition:
  config:
    merge:
      - local: ./roles/util.ign.json

storage:
  files:
    ## Set static hostname
    - path: /etc/hostname
      contents:
        inline: util-02.local
